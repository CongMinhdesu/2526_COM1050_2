class Solution:
    def isPalindrome(self, x: int):
        l = 0
        r = len(str(x))-1
        x = str(x)
        while l <= r:
            if x[l] == x[r]:
                l = l + 1
                r = r - 1
            else:
                return False
        return True
